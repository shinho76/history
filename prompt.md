첨부한 텍스트 파일("한국근현대사_텍스트_수정본_1.txt")을 입력받아서
"키워드 암기 플래시카드 웹앱"을 만들어줘.

[1. 텍스트 파일에서 내용 추출]
- 입력은 사진이 아니라, 이미 페이지별로 정리된 텍스트 파일이다.
  - `## 페이지 N-M (이미지 X-Y)` 형태의 구분자로 페이지(구간)가 나뉘어 있으므로,
    이 구분자를 기준으로 각 문단이 몇 페이지에서 나온 내용인지 식별한다.
  - 텍스트 중 `**굵게 표시된 부분**`은 원본 사진에서 이미 "다른 글자보다 눈에
    띄게 굵은 글씨"로 시각 분석되어 마킹된 결과이다. 따라서 별도의 OCR이나
    비전 기반 굵기 판별 없이, 이 마크다운 굵게 표시(`**...**`)를 그대로
    강조 표시 신호로 사용한다.
  - 텍스트 안에 OCR 오탈자로 보이는 부분(예: "심사 기제", "코민테른"의 오타,
    "산동반도"의 변형 등)이 있을 수 있다. 명백한 오탈자는 문맥상 자연스럽게
    교정하되, 임의로 의미를 바꾸지 않는다. 애매한 경우 원문 그대로 두고
    사용자가 검수 단계에서 직접 수정할 수 있게 한다.
  - 여러 구간(페이지)에 걸쳐 같은 키워드가 반복 등장하면 하나로 합치고,
    등장한 모든 페이지 번호를 기록한다.

[2. 키워드 및 정의 선정]
- `**...**`로 강조된 단어/구문을 키워드 후보로 삼되, 다음 기준으로 최종 목록을 만든다.
  - 단순 강조용 부사/조사(예: "매우", "반드시")나 문장 전체가 통째로 굵게
    표시된 경우처럼 학습 대상 단어로 보기 어려운 것은 제외하고, 그 안에서
    실제 핵심 용어(고유명사, 정책명, 사건명, 제도명 등)를 추려낸다.
  - 키워드당 "정의/설명"을 해당 키워드가 등장한 문단(주변 문맥)에서 함께
    추출한다(카드 뒷면에 보여줄 내용이 필요하므로 키워드만 있으면 안 됨).
- 결과를 {keyword, definition, sourcePage} 형태의 리스트(JSON)로 만든다.
  (sourcePage는 텍스트의 페이지 구분자에서 가져온 페이지 번호/범위)
- 최종 키워드 개수와 목록을 화면에서 확인/수정할 수 있게 한다(오탈자나 잘못
  뽑힌 키워드를 사용자가 직접 고칠 수 있어야 함).

[3. 룰렛(랜덤 출제) 로직]
- "아직 모른다고 표시된" 키워드 목록 중에서만 랜덤으로 하나를 선택한다.
  (이미 "안다"로 처리된 키워드는 출제 풀에서 제외 — 4번과 연결)
- 같은 키워드가 연속으로 두 번 나오지 않게 한다.
- 선택 연출은 슬롯머신/룰렛처럼 후보 키워드들이 빠르게 스쳐 지나가다가
  하나에서 멈추는 애니메이션으로 구현한다.

[4. 플래시카드 학습 (스와이프)]
- 카드 앞면: 키워드만 표시. 탭/클릭하면 뒷면(정의)이 보이도록 한다.
- 판정은 좌우 스와이프 방식을 기본으로 한다(모바일 학습 앱 표준):
  - 오른쪽 스와이프 = 안다 → "완료" 상태로 표시, 이후 룰렛 풀에서 제외
  - 왼쪽 스와이프 = 모른다 → "복습 대상"으로 유지, 룰렛 풀에 계속 포함
- 데스크탑에서는 버튼(⭕/❌) 또는 방향키로도 동일하게 조작 가능해야 한다.
- 학습 상태는 새로고침해도 유지되도록 로컬 저장소(localStorage)에 저장한다.
- "모른다"로 판정한 키워드는 언제든 다시 초기화(재학습)할 수 있는 버튼을 둔다.

[5. 진행률 표시]
- 상단에 진행률 바(Progress Bar)를 고정 표시한다.
- 진행률 = (안다로 표시된 키워드 수) / (전체 키워드 수) × 100%
- 숫자도 함께 표기한다(예: "12 / 40 완료").
- 전체 키워드를 다 알게 되면 완료 축하 화면을 보여준다.

[6. 기타 요구사항]
- 반응형으로 모바일/데스크탑 모두 자연스럽게 동작해야 한다.
- 키워드 목록과 학습 상태는 업로드한 텍스트 파일(세트) 단위로 구분해 저장한다.
- 흐름: 텍스트 파일 업로드(또는 앱에 내장) → 텍스트/키워드 자동 추출 →
  사용자가 키워드 목록 검수·수정 → 학습(룰렛+스와이프) 시작.
